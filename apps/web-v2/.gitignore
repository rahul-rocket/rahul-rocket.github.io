# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next
/out
next-env.d.ts
*.tsbuildinfo

# The base-path-aware preview tree built by scripts/serve-preview.mjs -- out/
# mounted at both / and /v2/ so the export's own URLs resolve. Rebuilt on every
# `pnpm start`, never edited by hand.
/.preview

# testing
/coverage
/playwright-report
/test-results
/.lighthouseci

# legacy create react app output — goes with src/ at the Phase 10 cutover
/build

# tool caches
/.cache

# misc
.DS_Store
Thumbs.db
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
